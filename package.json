{
  "name": "dash-logistic-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "Dash Logistic Design System — re-themed Dash UI atoms (npm library) + a live component gallery.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @dash-electric/logistic-docs",
    "build": "npm run build --workspace @dash-electric/logistic-docs",
    "preview": "npm run preview --workspace @dash-electric/logistic-docs",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
